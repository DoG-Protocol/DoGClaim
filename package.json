{
  "scripts": {
    "lint": "solhint contracts/**/*.sol",
    "build": "forge build",
    "test": "forge build && forge test -vvv",
    "coverage": "forge coverage --report summary --report lcov",
    "full": "bun lint && bun run test && bun coverage",
    "publish": "thirdweb publish"
  },
  "dependencies": {
    "@openzeppelin/contracts-upgradeable": "^5.0.2",
    "solhint": "^4.1.1",
    "thirdweb": "^0.13.43"
  }
}